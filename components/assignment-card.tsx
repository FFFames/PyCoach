import Link from "next/link";import { ArrowUpRight } from "lucide-react";import { Assignment } from "@/lib/types";
export function AssignmentCard({assignment,index}:{assignment:Assignment;index:number}){return <Link href={`/assignments/${assignment.id}`} className="group card flex min-h-64 flex-col p-6 hover:-translate-y-1 hover:border-moss/40"><div className="flex items-start justify-between"><span className="font-mono text-sm font-bold text-moss/60">0{index+1}</span><ArrowUpRight className="text-slate-300 group-hover:text-clay"/></div><div className="mt-auto"><span className="rounded-full bg-lime/45 px-3 py-1 text-[11px] font-black uppercase tracking-wider">{assignment.skill}</span><h3 className="mt-4 text-2xl font-black tracking-tight">{assignment.title}</h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-slate-600">{assignment.description}</p><p className="mt-5 text-xs font-bold text-slate-400">{assignment.difficulty} · {assignment.tests.length} tests</p></div></Link>}
