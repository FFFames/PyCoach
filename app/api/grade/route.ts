import { NextRequest,NextResponse } from "next/server";import { assignments } from "@/lib/data";import { gradeCode } from "@/lib/grader";import { updateMastery,BKT } from "@/lib/bkt";
export async function POST(request:NextRequest){try{const body=await request.json();if(typeof body.assignmentId!=="string"||typeof body.code!=="string")return NextResponse.json({error:"assignmentId and code are required."},{status:400});const assignment=assignments.find(a=>a.id===body.assignmentId);if(!assignment)return NextResponse.json({error:"Assignment not found."},{status:404});const result=await gradeCode(body.code,assignment);const mastery=updateMastery(BKT.initial,result.grade===100);return NextResponse.json({...result,mastery:Number(mastery.toFixed(4))})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Unable to grade submission."},{status:400})}}
